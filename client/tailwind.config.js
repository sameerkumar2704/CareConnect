export const content = [
    "./src/**/*.{js,ts,jsx,tsx}", // Add this for React + TS support
];
export const theme = {
    extend: {
        boxShadow: {
            cyanGlow: "0 0 30px rgba(71, 205, 191, 0.25)",
        },
    },
};
export const plugins = [];
